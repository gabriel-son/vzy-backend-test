export enum ROLES {
	USER = "user",
	ADMIN = "admin",
}

export enum SIGNUPTYPES {
	GOOGLE = "google",
	LOCAL = "local",
}

export enum STATUS {
	COMPLETED = "completed",
	FAILED = "failed",
	PENDING = "pending",
}
